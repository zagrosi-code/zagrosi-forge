# Auth and Identity

Inspect guards, identity/session storage, providers, error contracts, and negative
access tests. Decide proof/session ownership, expiry/replay/invalid-state failures,
linking policy, result shapes, and rollout/rollback.

Verify valid login, rejected credentials, expiry, ambiguous linking,
wrong-role/cross-account access, and redacted logs. Separate interactive login
from service credentials. Preserve trust-boundary validation and default-deny
authorization when simplifying code.
