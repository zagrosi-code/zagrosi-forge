# AI Agent App Scenario

Build an AI support agent that retrieves account documentation, drafts replies,
uses tool calls for ticket updates, and escalates low-confidence cases. The
plan should define prompt/version ownership, output schemas, evals, privacy,
tool permissions, fallback behavior, and cost/latency observability.
