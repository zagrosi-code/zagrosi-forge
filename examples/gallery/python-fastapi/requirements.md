# Python FastAPI Scenario

Add API-key authentication, request-scoped authorization, structured audit
logging, and OpenAPI documentation to a FastAPI service. The plan should
identify middleware, dependency injection, tests, secret handling, deployment
configuration, and rollback if API-key validation causes production failures.
