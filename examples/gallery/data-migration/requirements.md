# Data Migration Scenario

Migrate customer records from single-account ownership to organization
ownership without downtime. The plan should use expand/contract sequencing,
dual reads, backfill batches, verification queries, rollback constraints, and
cross-account access tests.
