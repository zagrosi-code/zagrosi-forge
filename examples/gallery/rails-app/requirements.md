# Rails App Scenario

Add account settings, email preference storage, and a safe migration from a
legacy users table to a profile table. The plan should use Rails migration
patterns, model validations, controller tests, view states, rollback limits,
and data backfill verification.
