# Go Service Scenario

Introduce a background job worker for usage aggregation in a Go service. The
plan should cover queue ownership, idempotent processing, retries, metrics,
alerting, data migration, integration tests, and deployment rollback.
