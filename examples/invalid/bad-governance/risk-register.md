# Bad Risk Fixture

| Risk | Fix |
|------|-----|
| OAuth callback creates a session on invalid state. | Add tests later. |

This fixture intentionally omits severity, likelihood, section, mitigation, and
verification columns.
