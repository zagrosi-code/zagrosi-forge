{
  "summary": {
    "depth": "lean",
    "request": "Add invoice('summary', items, customer) returning customer, subtotal, tax, total, and item_count (sum of quantities). Preserve all existing actions, exact exports, and error behavior.",
    "assertions": 633
  },
  "cleanup": {
    "depth": "standard",
    "request": "Simplify the existing invoice code and repair encountered duplication and mixed responsibilities. Preserve every existing public entrypoint, output, and error. Add meaningful regression coverage before cleanup.",
    "assertions": 507
  },
  "discount": {
    "depth": "deep",
    "request": "Add optional discount_percent=0 to invoice. Accept only integers 0..100 (reject bool and other types with ValueError). Reduce aggregate subtotal by floor(subtotal * percent / 100), then compute existing tax and exports from that subtotal. Zero discount preserves existing behavior. Include a compact deep plan with evidence, alternatives, risks, review, and tests.",
    "assertions": 2403
  },
  "godfile": {
    "fixture": "godfile",
    "oracle": "tools/godfile_trial_checks.py",
    "assertions": 75,
    "depth": "deep",
    "request": "Read prompt.md and complete its order-summary and module-boundary requirements."
  },
  "resume": {
    "depth": "standard",
    "request": "Resume the interrupted summary feature using .planning/progress.md. Complete invoice('summary', items, customer) with customer, subtotal, tax, total, and item_count (sum of quantities). Preserve existing exports/errors and previously added tests. Record remaining verification and completion.",
    "assertions": 633
  }
}
